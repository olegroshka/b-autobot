# Maven build output
target/

# IDE files
.idea/
*.iml
*.iws
*.ipr
.vscode/

# Claude Code internal state
.claude/

# OS artefacts
.DS_Store
Thumbs.db

# Playwright browser cache (downloaded separately with mvn test-compile -Dplaywright.install.skip=false)
playwright/

# Node.js runtime downloaded by frontend-maven-plugin (mvn verify -Dblotter.build.skip=false)
.node/

# PT-Blotter webapp dependencies (installed by frontend-maven-plugin)
src/test/webapp/node_modules/

# PT-Blotter Vite build output — regenerated by frontend-maven-plugin, not source-controlled.
# The pre-built fallback index.html IS committed; full Vite output is not.
src/test/resources/wiremock/__files/blotter/assets/

# Test evidence generated at runtime — not source-controlled
test-evidence/
