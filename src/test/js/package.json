{
  "name": "ag-grid-probes",
  "version": "1.0.0",
  "description": "AG Grid browser probes — tested in isolation with Jest + jsdom",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
